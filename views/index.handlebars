
     <h1 class="text-center mt-4">Eat Dat-Burger!</h1>
<div class="container">
  <div class="row"><div class="col-md-6 bg-primary pr-4">
    <div class="row"><div class="col-md-12"> <h2>Burgers That are not Devoured!</h2>     
      </div></div>
        
    <ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devour=true}}
    {{/unless}}
  {{/each}}
</ul>
   </div><div class="col-6-md bg-info"><h2>Burgers that are Devoured!</h2>
<ul>
    {{#each burgers}}
    {{#if devoured}}
    {{>burgers/burger-block devour=false}}
     {{/if}}
     {{/each}}
</ul></div></div>
<div class="row justify-content-center"><div class="col-12-md"><h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>


  <button type="submit">Add Burger</button>
</form>
</div></div>
</div>


