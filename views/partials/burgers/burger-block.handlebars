
<li>
	<div class="row">
<div class="col-md-8">{{burger_name}}</div>	
<div class="col-md-2"><button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devour}}">
		{{#if devoured}}order more!{{else}}devore!{{/if}}
	</button></div>	

<div class="col-md-2"><button class="delete-burger" data-id="{{this.id}}">DELETE!</button></div>	

</div>
</li>
